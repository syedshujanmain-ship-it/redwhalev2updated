// sourceFiles.ts - AUTO-GENERATED placeholder
// This file is regenerated during development. In production, it's lazy-loaded
// via dynamic import() so it doesn't bloat the main bundle (it's ~150MB).
export function getAllSourceFiles(): Record<string, string> {
  // This will be replaced by the actual content during development.
  // In production builds, this module is loaded on-demand only when
  // the user clicks "Download Source Code".
  return {};
}
